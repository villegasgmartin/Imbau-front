import { useEffect } from "react"
import { useDispatch } from "react-redux"

export default function Categorias (){
      // const dispatch = useDispatch()
      // useEffect(() => {
      //       dispatch()
      // },[])
      return(
            <div>
                 Creador de categorias y subcategorias
            </div>
      )
}